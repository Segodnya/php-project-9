# Database configuration
# For production, uncomment and set the DATABASE_URL:
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# In development, the app will use a local SQLite database by default

# Application settings
APP_ENV=development
DEBUG=true
LOG_ERRORS=true
